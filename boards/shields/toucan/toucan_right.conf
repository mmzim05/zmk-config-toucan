CONFIG_ZMK_POINTING=y

CONFIG_BT_CTLR_TX_PWR_PLUS_8=y

# Fastest BLE connection interval (7.5ms), no skipped events — prevents trackpad
# events from batching over BLE which causes cursor teleport on finger lift
CONFIG_BT_PERIPHERAL_PREF_MIN_INT=6
CONFIG_BT_PERIPHERAL_PREF_MAX_INT=6
CONFIG_BT_PERIPHERAL_PREF_LATENCY=0
CONFIG_BT_PERIPHERAL_PREF_TIMEOUT=600

# The pinnacle driver also requires this to include sleep code
CONFIG_ZMK_MOUSE=y

CONFIG_ZMK_BATTERY_REPORTING=y
# CONFIG_ZMK_SPLIT_BLE_CENTRAL_BATTERY_LEVEL_FETCHING=y
# CONFIG_ZMK_SPLIT_BLE_CENTRAL_BATTERY_LEVEL_PROXY=y

# Touchpad autosleep (EN_SLEEP): tried this, reverted. zmk_pinnacle_idle_sleeper.c
# (cirque-input-module) flips it on after CONFIG_ZMK_IDLE_TIMEOUT (30s), but once on,
# the chip's own hardcoded Sleep Interval register (set to 255 in input_pinnacle.c's
# init, not exposed via Kconfig/DT) governs how often it wakes to check for a touch —
# not a flat ~300ms. A touch landing between checks gets missed until the next one,
# so in practice it took several seconds of moving a finger around before the cursor
# responded again. Worse than the deep-sleep-only tradeoff, so left off. Would need
# forking cirque-input-module to make the Sleep Interval configurable/short to revisit.
# CONFIG_ZMK_INPUT_PINNACLE_IDLE_SLEEPER=y

# Enter deep sleep https://zmk.dev/docs/features/low-power-states
CONFIG_ZMK_SLEEP=y

# Right half is the peripheral: its "reconnect" is just the peripheral<->central
# split link re-pairing automatically in the background, not the user re-bonding
# to the host, so the "avoid deep sleep, it's annoying to reconnect" tradeoff that
# justifies a long timeout on the central does not apply here. Sleep aggressively
# to save battery — 5 minutes idle before deep sleep.
CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=300000

# Also gates when the Pinnacle chip itself sleeps (CONFIG_ZMK_INPUT_PINNACLE_IDLE_SLEEPER
# above) — the chip sleeps as soon as ZMK goes idle, i.e. 30s after last activity, not
# a separate 5s timer.
CONFIG_ZMK_IDLE_TIMEOUT=30000

# Soft Off: tried, reverted — it's what broke waking the right half from deep sleep
# (only the reset button worked, not real key presses). ZMK's own Kconfig
# (`config ZMK_SLEEP` -> `select ZMK_PM_DEVICE_SUSPEND_RESUME`) already gives us
# PM_DEVICE + PM_DEVICE_SUSPEND_RESUME (the Cirque suspend/resume notifications) via
# CONFIG_ZMK_SLEEP=y alone — Soft Off was never needed for that, the old comment here
# was wrong. Soft Off is also unreachable in this config (no &soft_off binding in the
# keymap), and per ZMK docs it's "significantly more restrictive in the sources which
# can wake it" than regular deep sleep — it needs an explicit zmk,soft-off-wakeup-sources
# devicetree node to allow anything but the reset button to wake it, which this repo
# never defined. Leave this off; regular deep sleep's wakeup-source on kscan0
# (toucan.dtsi) already wakes on a real key press correctly.
# CONFIG_ZMK_PM_SOFT_OFF=y

# Dedicated gesture work queue stack — 6KB gives margin for ZMK kscan→split→BLE call chain
# including deep error-handling paths under BLE retransmit pressure at long range
CONFIG_ZMK_INPUT_PERIPHERAL_GESTURE_WORKQ_STACK_SIZE=6144

# INPUT THREAD calls zmk_kscan_touch_report→kscan_composite→ZMK keyboard→bt_gatt_notify on
# every touch start.  Under poor BLE signal, bt_gatt_notify's error-handling path is deeper
# than the success path; with the default 1024B stack this overflows at range → HardFault.
# Left half already sets this to 4096 for the same reason.
CONFIG_INPUT_THREAD_STACK_SIZE=4096

# More ACL TX buffers so retransmissions at long range don't exhaust the pool and cause
# bt_gatt_notify to return -ENOMEM, which stresses ZMK's split state machine.
CONFIG_BT_BUF_ACL_TX_COUNT=10

