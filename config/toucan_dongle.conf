CONFIG_ZMK_POINTING=y

CONFIG_ZMK_MOUSE=y

# Smooth (high-resolution) scrolling instead of notchy single-line jumps,
# for hosts that support HID Resolution Multipliers
CONFIG_ZMK_POINTING_SMOOTH_SCROLLING=y
